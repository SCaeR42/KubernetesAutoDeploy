php-helloworld установлен (релиз: {{ .Release.Name }}, namespace: {{ .Release.Namespace }}).

Проверить поды:
  kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }} -n {{ .Release.Namespace }}

{{- if eq .Values.service.type "LoadBalancer" }}
Внешний IP появится через 1-3 минуты:
  kubectl get svc {{ include "php-helloworld.fullname" . }}-svc -n {{ .Release.Namespace }} -w
{{- else if eq .Values.service.type "NodePort" }}
Доступ через NodePort:
  kubectl get svc {{ include "php-helloworld.fullname" . }}-svc -n {{ .Release.Namespace }} -o jsonpath='{.spec.ports[0].nodePort}'
{{- else }}
Service типа {{ .Values.service.type }} - настройте доступ (Ingress/port-forward) отдельно.
{{- end }}
